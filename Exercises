#Задача 1. Найти итоговую сумму за покупку со скидкой по условиям:
    
    #price - базовая цена товара
    
    #quantity - количество штук в заказе
    
    #card_type - тип скидочной карты


    Если карты нет (любое другое значение строки), дополнительной скидки нет.

Если количество товара 5 штук или больше, сделать оптовую скидку 10% на всю сумму.

После этого применить скидку по карте:
Если card_type == "Gold", отниму еще 15%.

Если card_type == "Silver", отними еще 7%.

Если карты нет (любое другое значение строки), дополнительной скидки нет.

Функция должна возвращать (return) итоговую сумму, округленную до двух знаков после запятой.
    
#Решение:

def calculate_total(price, quantity, card_type):
    
 { 
     {
         list1 = [1, 2, 3, 4, 5];
     
         price = list1;
         
     }
     
     
     {
         def quantity = price * count
         
         list2 = [2, 3, 4, 5];
         count = list2;
     }
     
     
     {
         card_type = list3;
         list3 = ["Gold", "Silver"];
     }
 }
     
     if card_type = "Gold"
        return calculate_total * 0.85


     if card_type = "Silver" 
        return calculate_total * 0.93
